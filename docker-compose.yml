services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monochrome-twilio-calling
    restart: unless-stopped
    ports:
      - "5050:5050"
    env_file:
      - .env
    volumes:
      # Persist runtime data (db.json) across container restarts
      - ./data:/app/data
